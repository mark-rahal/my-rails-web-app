<div class="create">
  <div class="container">

    <!-- Your code here -->
    <%= form_for(@comment) do |f| %>
      <div class="field">
        <%= f.label :comment %><br>
        <%= f.text_area :content %>
      </div>
      <div class="actions">
        <%= f.submit "Create" %>
      </div>
    <% end %>
  </div>
</div>
